---
title: Figma plugin — token sync
description: Bidirectional sync between Figma variables and kj theme files. Edit in one place, see it in the other.
version: tba
date: Q1 2027
category: theme
status: idea
issues: 3
prs: 0
candor: Cool if it works. Hard to maintain a plugin alongside a UI library.
---

Edit a token in `/theme-generator`, see it in your Figma variables. Tweak a Figma variable, push it back to a `.css` theme file. Probably ships as a companion plugin (`@kouji-ui/figma`) rather than bundled into the main package.
