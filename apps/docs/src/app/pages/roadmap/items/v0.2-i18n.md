---
title: i18n + locale system
description: One provider feeds locale-aware components (DatePicker, NumberInput, TimePicker, RTL switch) AND a typed translation store for every visible string.
version: v0.2.0
date: Sep 2026
category: a11y
status: next
---

Two halves of the same problem, one ergonomic surface:

**Locale-aware components.** `provideKjLocale({ locale, direction, calendar })` (or the implicit `Intl.Locale` resolved from the browser) propagates through DI. Every locale-sensitive primitive picks it up — DatePicker, NumberInput, TimePicker, the RTL switch, all `Intl`-backed formatting. No per-component locale prop drilling.

**Translation strings.** A typed `KjTranslate` directive + `KjTranslateService` for the visible-text surface: built-in keys for every internal label (toast close, dialog dismiss, pagination "page X of Y", a11y live-region announcements), pluggable per-locale message catalogs, ICU-style placeholders, and a fall-through to the source language so missing translations never blank out the UI. Catalogs ship as tree-shakable `import` modules so you only bundle the languages you actually use.
